import { Form } from "@/src/app/[locale]/auth/components/Form";

export default function Auth() {
    return (
        <main className="flex justify-center items-center h-screen bg-landingBG bg-cover">
            <Form />
        </main>
    );
}
