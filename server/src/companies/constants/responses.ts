import { HttpStatus } from "@nestjs/common"
import { error } from "console"

export const RESPONSES={
    BAD_REQUEST: 'Falta informacion.',
    NOT_FOUND: 'No se encontro la empresa.',
    INTERNAL_SERVER_ERROR: 'Error interno del servidor.'
}